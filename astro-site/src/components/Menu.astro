<div class="menu-root" data-menu>
  <!-- ボタン -->
  <button class="menu-button" type="button" aria-label="メニュー" aria-expanded="false" data-menu-button>
    <span></span>
    <span></span>
    <div class="menu-label" aria-hidden="true"></div>
  </button>

  <!-- オーバーレイ -->
  <div class="overlay" data-menu-overlay></div>

  <!-- メニュー本体 -->
  <nav class="menu" aria-label="サイトメニュー" data-menu-panel>
    <div class="menu-header">
      <img src="https://raw.githubusercontent.com/you527h/my-juku-site/main/astro-site/public/images/juku-logo.png" alt="logo"/>
      <div class="menu-header-combi-text">
        <span class="menu-header-text-sub">家庭教師・個別指導・集団授業</span>
        <span class="menu-header-text-main">プロ講師指導塾</span>
      </div>
    </div>

    <div class="menu-list">
      <a class="menu-item" href="/private-teacher">
        <div class="menu-item-main">家庭教師</div>
        <div class="menu-item-sub">全科目・講座受講可能</div>
      </a>

      <a class="menu-item" href="/mass-class">
        <div class="menu-item-main">集団授業</div>
        <div class="menu-item-sub">英語・数学をより深く学ぶ</div>
      </a>

      <a class="menu-item" href="/one-class">
        <div class="menu-item-main">個別指導</div>
        <div class="menu-item-sub">1対1で徹底したサポート</div>
      </a>

      <a class="menu-item" href="/course">
        <div class="menu-item-main">講座・料金</div>
        <div class="menu-item-sub">開講講座やカリキュラム例はこちら</div>
      </a>

      <a class="menu-item" href="/about">
        <div class="menu-item-main">プロ講師指導塾とは</div>
        <div class="menu-item-sub">代表からの挨拶</div>
      </a>

      <a class="menu-item" href="/how-to-enter">
        <div class="menu-item-main">入塾までの流れ</div>
        <div class="menu-item-sub">入塾に関する無料相談受付中</div>
      </a>

      <a class="menu-item" href="/contact">
        <div class="menu-item-main">お問い合わせ</div>
        <div class="menu-item-sub">電話・LINE・メールで受付中</div>
      </a>
    </div>
  </nav>
</div>

  
<style>
  /* ===== ボタン ===== */
  .menu-button {
    display: flex;
    position: fixed;
    right: 10px;
    top: 10px;
    cursor: pointer;
    z-index: 1100;
    width: 50px;
    height: 50px;
    padding: 10px 10px 6px;
    border-radius: 6px;
    flex-direction: column;
    justify-content: space-between;
    background: rgb(10,10,10);
    border: none;
  }
  .menu-button span {
    height: 3px;
    width: 100%;
    background: #fff;
    border-radius: 2px;
    transition: transform 400ms ease, opacity 0.25s ease;
  }
  .menu-label::before {
    content: "Menu";
    display: block;
    font-size: 10px;
    color: #fff;
    text-align: center;
    margin-top: -2px;
    letter-spacing: 0.06em;
  }

  /* ×アニメ */
  .menu-root.is-open .menu-label::before { content: "Close"; }
  .menu-root.is-open .menu-button span:nth-child(1) {
    transform: translateY(7px) rotate(45deg);
  }
  .menu-root.is-open .menu-button span:nth-child(2) {
    transform: translateY(-3px) rotate(-45deg);
  }

  /* ===== オーバーレイ ===== */
  .overlay {
    position: fixed;
    inset: 0;
    background: rgba(0,0,0,0.9);
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.5s ease;
    z-index: 800;
  }
  .menu-root.is-open .overlay {
    opacity: 1;
    pointer-events: auto;
  }

  /* ===== メニュー本体 ===== */
  .menu {
    position: fixed;
    inset: 0;
    background: #fff;
    transform: translateY(-100%);
    transition: transform 400ms ease;
    z-index: 900;
    overflow: auto;
    -webkit-overflow-scrolling: touch;
    padding: 18px 16px 30px;
  }

  .menu-root.is-open .menu {
    transform: translateY(0);
  }

  .menu-header {
    display: flex;
    padding: 6px 4px 14px;
    border-bottom: 2px solid rgb(201,168,44);
    margin-bottom: 14px;
  }

.menu-header > img {
  height: 40px;
  width: 40px;
  border-radius: 50px;
  border: 1px solid rgb(201,168,44);
}

.menu-header-combi-text {
  display: flex;
  flex-direction: column;
  line-height: 1.3;
  margin: 4px 0 0 10px;
  font-weight: 100;
}

.menu-header-text-main{
  font-size: 20px;
  color: #000;
  display: block;
}

.menu-header-text-sub{
  margin-left: 3px;
  font-size: 10px;
  color: #000;
  display: block;
}

  .menu-list {
    display: grid;
    gap: 12px;
  }
  .menu-item {
    display: block;
    text-decoration: none;
    color: #000;
    border-radius: 10px;
    padding: 12px 12px 12px 14px;
    background: #fff;
    border: 2px solid rgba(0,0,0,0.08);
    position: relative;
  }

  /* 左のゴールドバー */
  .menu-item::before {
    content: "";
    position: absolute;
    left: 5px;
    top: 8px;
    bottom: 8px;
    width: 4px;
    background: rgb(201,168,44);
    border-radius: 3px;
  }

  .menu-item-main {
    font-size: 16px;
    font-weight: 900;
    line-height: 1.2;
  }
  .menu-item-sub {
    font-size: 12px;
    opacity: 0.85;
    margin-top: 4px;
    line-height: 1.3;
  }

  .menu-item:active {
    transform: translateY(1px);
  }
</style>

<script>
  // 「メニューだけ」動かす。slider等の有無で止まらない。
  (() => {
    const root = document.querySelector("[data-menu]");
    if (!root) return;

    const button = root.querySelector("[data-menu-button]");
    const panel  = root.querySelector("[data-menu-panel]");
    const overlay = root.querySelector("[data-menu-overlay]");

    if (!button || !panel || !overlay) return;

    const open = () => {
      root.classList.add("is-open");
      button.setAttribute("aria-expanded", "true");
      document.body.classList.add("menu-open");
    };

    const close = () => {
      root.classList.remove("is-open");
      button.setAttribute("aria-expanded", "false");
      document.body.classList.remove("menu-open");
    };

    const toggle = () => {
      root.classList.contains("is-open") ? close() : open();
    };

    button.addEventListener("click", toggle);
    overlay.addEventListener("click", close);

    // メニュー内リンク押したら閉じる（好みで）
    panel.addEventListener("click", (e) => {
      const a = e.target.closest("a");
      if (a) close();
    });

    // Escで閉じる
    window.addEventListener("keydown", (e) => {
      if (e.key === "Escape") close();
    });
  })();
</script>
